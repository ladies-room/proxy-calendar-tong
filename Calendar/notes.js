// hooks:
import React, { useState, useEffct, othreHookThings } from 'react'

const [product, setProduct] = useStatee([]);
// product: this.state.product;
// setProduct: this.setState({..});
// useState([]): initial state

// setState: setProduct(res.data)

// componentDidMount():
// useEffct(() => {getProduct()}, []);
